diff --git a/src/tools/tool_shell.c b/src/tools/tool_shell.c
index 1acc875..a98bddb 100644
--- a/src/tools/tool_shell.c
+++ b/src/tools/tool_shell.c
@@ -145,6 +145,13 @@ static int is_blocked(const char *cmd)
     if (has_shell_meta(cmd))
         return 1;
 
+    /* FlyReflex: exact, simulation-only arbiter operations, not a general
+     * permission to invoke arbitrary FlyReflex commands. */
+    if (strcmp(cmd, "flyreflex guard query_flyreflex_status") == 0 ||
+        strcmp(cmd, "flyreflex guard issue_agent_command FORWARD") == 0)
+        return 0;
+
+
     /* Extract the first token (command name) */
     char first[64];
     int i = 0;
@@ -508,14 +515,15 @@ int tool_run_shell_execute(const char *input_json, char *output, size_t output_s
 #endif
     }
 
-    cJSON_Delete(root);
-
     if (ret != OK) {
         snprintf(output, output_size, "{\"error\":\"Command failed: %s\"}", command);
+        cJSON_Delete(root);
         free(cmd_buf);
         return ERROR;
     }
 
+    cJSON_Delete(root);
+
     /* Build JSON result */
     cJSON *result = cJSON_CreateObject();
     cJSON_AddNumberToObject(result, "exit_code", 0);
